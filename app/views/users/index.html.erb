<h1>Users</h1>

<table>
  <thead>
  <tr>
    <th>User Name</th>
    <th>Email</th>
    <th>SIS ID</th>
    <th>Canvas ID</th>
    <th>Enrollments</th>
    <th></th>
  </tr>
  </thead>

  <tbody>
  <% @users.each do |user| %>
    <tr>
      <td><%= link_to user.name, user %></td>
      <td><%= user.email %></td>
      <td><%= user.sis_id %></td>
      <td><%= user.canvas_id ? link_to("#{user.canvas_id} ⧉", "#{canvas_path}/users/#{user.canvas_id}") : '' %></td>
      <td><%= user.enrollments.size %></td>
      <td><%= link_to 'Edit', edit_user_path(user) %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Sync SIS Users', sync_sis_users_path, data: { toggle: 'tooltip'}, title: 'Detect new/updated users from OnCampus.' %>
|
<%= link_to 'Sync Canvas Users', sync_canvas_users_path, class: 'slow btn btn-primary m-2', data: { toggle: 'tooltip'}, title: 'Create new Canvas users where canvas_id is missing, or record canvas user_id if they already exist. ' %>
<br>
<%= link_to 'Full Sync', full_sync_path, class: 'slow btn btn-primary m-2', data: { toggle: 'tooltip'}, title: 'Sync Everything for All Courses where sync=true.' %>
