<p id="notice"><%= notice %></p>

<h1>Terms</h1>

<table>
  <thead>
  <tr>
    <th>Name</th>
    <th>Canvas ID</th>
    <th>Start</th>
    <th>End</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  </thead>

  <tbody>
  <% @terms.each do |term| %>
    <tr>
      <td><%= term.name %></td>
      <td><%= term.canvas_id %></td>
      <td><%= format_date term.start %></td>
      <td><%= format_date term.end %></td>
      <td><%= link_to 'Show', term %></td>
      <td><%= link_to 'Edit', edit_term_path(term) %></td>
      <td><%= link_to 'Destroy', term, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<%= link_to 'New Term', new_term_path %>

<br>

<%= link_to 'Refresh Canvas Terms', refresh_canvas_terms_path, data: { method: :put } %> |
<%= link_to 'Refresh SIS Terms', refresh_sis_terms_path, data: { method: :put } %>
